# Secrets & local config — NEVER commit
.env

# Runtime data written by the bot (or by maintenance scripts run locally)
events.jsonl
tracked_cache.json
dashboard_msg.json
missing_state.json
progress.json
spool.jsonl
*.out

# Generated per-worker calendar exports (export_calendars.py) — contain real
# worker names + attendance/sick/vacation data. Never commit these.
calendars/
calendars_*/
*.xlsx

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
